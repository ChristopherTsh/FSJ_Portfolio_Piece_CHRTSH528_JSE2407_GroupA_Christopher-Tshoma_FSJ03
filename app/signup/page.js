import Signup from '../components/Signup';

/**
 * SignupPage component for rendering the signup interface.
 * 
 * @component
 * @returns {JSX.Element} The rendered SignupPage component.
 */
const SignupPage = () => {
  return <Signup />;
};

export default SignupPage;
