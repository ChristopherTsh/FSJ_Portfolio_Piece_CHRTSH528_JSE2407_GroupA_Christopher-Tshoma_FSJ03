import Login from '../components/Login';

/**
 * LoginPage component for rendering the login interface.
 * 
 * @component
 * @returns {JSX.Element} The rendered LoginPage component.
 */
const LoginPage = () => {
  return <Login />;
};

export default LoginPage;
